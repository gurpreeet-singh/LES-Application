[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd apps/api && node --import tsx src/demo-server.ts"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
