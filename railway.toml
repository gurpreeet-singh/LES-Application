[build]
builder = "NIXPACKS"
buildCommand = "echo 'Skip build — using tsx runtime'"

[deploy]
startCommand = "cd apps/api && npx tsx src/demo-server.ts"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
